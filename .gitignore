# macOS
.DS_Store

# Ferramentas / config local
.claude/

# Assets gerados por IA (pesados) — fora do repo
marca/fotos/
marca/personagem/
marca/produtos/

# Config MCP local
.mcp.json

# Ensaio da fachada: saídas brutas (pesadas) fora do repo
fachada/geradas/
